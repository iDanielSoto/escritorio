import { useEffect } from "react";

export const useInactivityTimer = () => {
    useEffect(() => {
        // En un futuro este hook controlará la inactividad real.
        // Se ha movido la opción de reseteo a la interfaz gráfica.
    }, []);
};
